# Contributing

Keep the plugin native to OMP. Do not add Claude hooks, `pi-yaml-hooks`, MCP servers, or an alternate routing path.

Before a pull request:

```text
pnpm run check
```

Changes to runtime pins require refreshed lock digests, evidence, and a release note.
